import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";

import { IcLoadingIndicator } from "../components";

import readme from "../../../web-components/src/components/ic-loading-indicator/readme.md";

<Meta title="Loading" component={IcLoadingIndicator} />

<Description markdown={readme} />

export const defaultArgs = {
  description: "Loading",
  fullWidth: false,
  labelDuration: 8000,
  size: "medium",
  type: "circular",
  label: [],
  theme: "inherit",
  monochrome: false,
};

export const defaultArgTypes = {
  label: {
    control: { type: "text" },
  },
  size: {
    options: ["small", "medium", "large", "icon"],
    control: { type: "radio" },
  },
  type: {
    options: ["circular", "linear"],
    control: { type: "radio" },
  },
  theme: {
    options: ["inherit", "light", "dark"],
    control: { type: "inline-radio" },
  },
  label: {
    control: { type: "array" },
  }
};

### Circular (determinate)

<Canvas>
  <Story name="Circular (indeterminate)">
    <IcLoadingIndicator label="Loading..." />
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Circular (determinate)"
  >
    <IcLoadingIndicator
      progress="30"
      min="0"
      max="100"
      label="Loading..."
    />
  </Story>
</Canvas>

### Circular long label

<Canvas>
  <Story name="Circular long label">
    <IcLoadingIndicator label="This is a label for a loading indicator that is indeterminate and should wrap onto more than one line when it gets too long" />
  </Story>
</Canvas>

### Circular variants (indeterminate)

<Canvas>
  <Story name="Circular variants (indeterminate)">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Small" size="small" />
      <IcLoadingIndicator label="Default" />
      <IcLoadingIndicator label="Large" size="large" />
    </div>
  </Story>
</Canvas>

### Circular variants (determinate)

<Canvas>
  <Story
    name="Circular variants (determinate)"
  >
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Small"
        size="small"
      />
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Default"
      />
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Large"
        size="large"
      />
    </div>
  </Story>
</Canvas>

### Icon

<Canvas>
  <Story name="Icon (circular)">
    <IcLoadingIndicator size="icon" />
  </Story>
</Canvas>

### Linear (indeterminate)

<Canvas>
  <Story name="Linear (indeterminate)">
    <IcLoadingIndicator label="Loading..." type="linear" />
  </Story>
</Canvas>

### Linear (determinate)

<Canvas>
  <Story
    name="Linear (determinate)"
  >
    <IcLoadingIndicator
      progress="30"
      min="0"
      max="100"
      label="Loading..."
      type="linear"
    />
  </Story>
</Canvas>

### Linear long label

<Canvas>
  <Story name="Linear long label">
    <IcLoadingIndicator type="linear" label="This is a label for a loading indicator that is indeterminate and should wrap onto more than one line when it gets too long" />
  </Story>
</Canvas>

### Linear variants (indeterminate)

<Canvas>
  <Story name="Linear variants (indeterminate)">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Small" size="small" type="linear" />
      <IcLoadingIndicator label="Default" type="linear" />
      <IcLoadingIndicator label="Large" size="large" type="linear" />
      <IcLoadingIndicator
        label="Full-width"
        size="medium"
        type="linear"
        fullWidth={true}
      />
    </div>
  </Story>
</Canvas>

### Linear variants (determinate)

<Canvas>
  <Story
    name="Linear variants (determinate)"
  >
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Small"
        size="small"
        type="linear"
      />
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Default"
        type="linear"
      />
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Large"
        size="large"
        type="linear"
      />
      <IcLoadingIndicator
        progress="30"
        min="0"
        max="100"
        label="Full-width"
        size="medium"
        type="linear"
        fullWidth={true}
      />
    </div>
  </Story>
</Canvas>

### Circular dark

<Canvas>
  <Story name="Circular dark" parameters={{ backgrounds: { default: "dark" } }}>
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Small" size="small" theme="dark" />
      <IcLoadingIndicator label="Default" theme="dark" />
      <IcLoadingIndicator label="Large" size="large" theme="dark" />
    </div>
  </Story>
</Canvas>

### Icon dark (circular)

<Canvas>
  <Story
    name="Icon dark (circular)"
    parameters={{ backgrounds: { default: "dark" } }}
  >
    <IcLoadingIndicator size="icon" theme="dark" />
  </Story>
</Canvas>

### Linear dark

<Canvas>
  <Story name="Linear dark" parameters={{ backgrounds: { default: "dark" } }}>
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Default" type="linear" theme="dark" />
      <IcLoadingIndicator label="Large" size="large" type="linear" theme="dark" />
      <IcLoadingIndicator
        label="Full-width"
        type="linear"
        fullWidth={true}
        theme="dark"
      />
    </div>
  </Story>
</Canvas>

### Monochrome

<Canvas>
  <Story
    name="Monochrome"
    parameters={{ loki: { skip: true } }}
  >
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        label="Light"
        monochrome
      ></IcLoadingIndicator>
      <div style={{ backgroundColor: "black" }}>
        <IcLoadingIndicator
          label="Dark"
          theme="dark"
          monochrome
        ></IcLoadingIndicator>
      </div>
      <IcLoadingIndicator
        size="icon"
        monochrome
      ></IcLoadingIndicator>
      <div style={{ backgroundColor: "black", display: "flex", justifyContent: "center" }}>
        <IcLoadingIndicator
          size="icon"
          theme="dark"
          monochrome
        ></IcLoadingIndicator>
      </div>
      <IcLoadingIndicator
        label="Light"
        type="linear"
        monochrome
      ></IcLoadingIndicator>
      <div style={{ backgroundColor: "black", padding: "var(--ic-space-xs)" }}>
        <IcLoadingIndicator
          label="Dark"
          type="linear"
          theme="dark"
          monochrome
        ></IcLoadingIndicator>
      </div>
    </div>
  </Story>
</Canvas>

### No label

<Canvas>
  <Story name="No label">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator description="Loading" />
      <IcLoadingIndicator description="Loading" type="linear" />
    </div>
  </Story>
</Canvas>

### Changing label

<Canvas>
  <Story name="Changing label">
    <IcLoadingIndicator
      label={["Loading...", "Still loading..."]}
      labelDuration="4000"
    />
  </Story>
</Canvas>

### Progress indicator for compact step

<Canvas>
  <Story
    name="Progress indicator for compact step"
    parameters={{ loki: { skip: true } }}
  >
    <IcLoadingIndicator
      size="small"
      inner-label="3"
      progress="30"
      min="0"
      max="100"
    />
  </Story>
</Canvas>

### Playground - indeterminate

<Canvas>
  <Story name="Playground - indeterminate"
    args={defaultArgs}
    argTypes={defaultArgTypes}
  >
    {(args) => (
        <div style={{ backgroundColor: args.theme === "dark" ? "#393939" : "#FFFFFF", padding: "16px" }}>
          <IcLoadingIndicator
            monochrome={args.monochrome}
            theme={args.theme}
            description={args.description}
            fullWidth={args.fullWidth}
            labelDuration={args.labelDuration}
            size={args.size}
            type={args.type}
            label={args.label}
          />
        </div>
      )}
  </Story>
</Canvas>

### Playground - determinate

<Canvas>
  <Story name="Playground - determinate"
    args={{
      ...defaultArgs,
      max: 100,
      min: 0,
      progress: 30,
    }}
    argTypes={defaultArgTypes}
  >
    {(args) => (
        <div style={{ backgroundColor: args.theme === "dark" ? "#393939" : "#FFFFFF", padding: "16px" }}>
          <IcLoadingIndicator
            monochrome={args.monochrome}
            theme={args.theme}
            description={args.description}
            fullWidth={args.fullWidth}
            labelDuration={args.labelDuration}
            size={args.size}
            type={args.type}
            label={args.label}
            progress={args.progress}
            min={args.min}
            max={args.max}
          />
        </div>
      )}
  </Story>
</Canvas>
