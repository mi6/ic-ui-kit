import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";

import { IcLoadingIndicator } from "../components";
import readme from "../../../web-components/src/components/ic-loading-indicator/readme.md";

<Meta title="React Components/Loading" component={IcLoadingIndicator} />

<Description markdown={readme} />

### Circular (determinate)

<Canvas>
  <Story name="Circular (indeterminate)">
    <IcLoadingIndicator label="Loading..."></IcLoadingIndicator>
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Circular (determinate)"
    argTypes={{
      progress: { type: { name: 'number' }, defaultValue: 30 },
      min: { type: { name: 'number' }, defaultValue: 0 },
      max: { type: { name: 'number' }, defaultValue: 100 },
    }}
  >
    {(args) => (
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Loading..."
      ></IcLoadingIndicator>
    )}
  </Story>
</Canvas>

### Circular variants (indeterminate)

<Canvas>
  <Story name="Circular variants (indeterminate)">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Small" size="small"></IcLoadingIndicator>
      <IcLoadingIndicator label="Default"></IcLoadingIndicator>
      <IcLoadingIndicator label="Large" size="large"></IcLoadingIndicator>
    </div>
  </Story>
</Canvas>

### Circular variants (determinate)

<Canvas>
  <Story name="Circular variants (determinate)"
    argTypes={{
      progress: { type: { name: 'number' }, defaultValue: 30 },
      min: { type: { name: 'number' }, defaultValue: 0 },
      max: { type: { name: 'number' }, defaultValue: 100 },
    }}
  >
    {(args) => (<div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Small"
        size="small"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Default"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Large"
        size="large"
      ></IcLoadingIndicator>
    </div>)}
  </Story>
</Canvas>

### Icon

<Canvas>
  <Story name="Icon (circular)">
    <IcLoadingIndicator size="icon"></IcLoadingIndicator>
  </Story>
</Canvas>

### Linear (indeterminate)

<Canvas>
  <Story name="Linear (indeterminate)">
    <IcLoadingIndicator label="Loading..." type="linear"></IcLoadingIndicator>
  </Story>
</Canvas>

### Linear (determinate)

<Canvas>
  <Story name="Linear (determinate)"
    argTypes={{
      progress: { type: { name: 'number' }, defaultValue: 30 },
      min: { type: { name: 'number' }, defaultValue: 0 },
      max: { type: { name: 'number' }, defaultValue: 100 },
    }}
  >
    {(args) => (<IcLoadingIndicator
      progress={args.progress}
      min={args.min}
      max={args.max}
      label="Loading..."
      type="linear"
    ></IcLoadingIndicator>)}
  </Story>
</Canvas>

### Linear variants (indeterminate)

<Canvas>
  <Story name="Linear variants (indeterminate)">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        label="Small"
        size="small"
        type="linear"
      ></IcLoadingIndicator>
      <IcLoadingIndicator label="Default" type="linear"></IcLoadingIndicator>
      <IcLoadingIndicator
        label="Large"
        size="large"
        type="linear"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        label="Full-width"
        size="default"
        type="linear"
        variant="full-width"
      ></IcLoadingIndicator>
    </div>
  </Story>
</Canvas>

### Linear variants (determinate)

<Canvas>
  <Story name="Linear variants (determinate)"
    argTypes={{
      progress: { type: { name: 'number' }, defaultValue: 30 },
      min: { type: { name: 'number' }, defaultValue: 0 },
      max: { type: { name: 'number' }, defaultValue: 100 },
    }}
  >
    {(args) => (<div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Small"
        size="small"
        type="linear"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Default"
        type="linear"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Large"
        size="large"
        type="linear"
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        progress={args.progress}
        min={args.min}
        max={args.max}
        label="Full-width"
        size="default"
        type="linear"
        variant="full-width"
      ></IcLoadingIndicator>
    </div>)}
  </Story>
</Canvas>

### Circular dark

<Canvas>
  <Story name="Circular dark" parameters={{ backgrounds: { default: "dark" } }}>
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator label="Small" size="small" dark></IcLoadingIndicator>
      <IcLoadingIndicator label="Default" dark></IcLoadingIndicator>
      <IcLoadingIndicator label="Large" size="large" dark></IcLoadingIndicator>
    </div>
  </Story>
</Canvas>

### Icon dark (circular)

<Canvas>
  <Story
    name="Icon dark (circular)"
    parameters={{ backgrounds: { default: "dark" } }}
  >
    <IcLoadingIndicator size="icon" dark></IcLoadingIndicator>
  </Story>
</Canvas>

### Linear dark

<Canvas>
  <Story name="Linear dark" parameters={{ backgrounds: { default: "dark" } }}>
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator
        label="Default"
        type="linear"
        dark
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        label="Large"
        size="large"
        type="linear"
        dark
      ></IcLoadingIndicator>
      <IcLoadingIndicator
        label="Full-width"
        type="linear"
        variant="full-width"
        dark
      ></IcLoadingIndicator>
    </div>
  </Story>
</Canvas>

### No label

<Canvas>
  <Story name="No label">
    <div style={{ display: "grid", gap: "50px" }}>
      <IcLoadingIndicator name="Loading"></IcLoadingIndicator>
      <IcLoadingIndicator name="Loading" type="linear"></IcLoadingIndicator>
    </div>
  </Story>
</Canvas>

### Changing label

<Canvas>
  <Story name="Changing label">
    <IcLoadingIndicator
      label="Loading.../Still loading..."
      labelDuration="4000"
    ></IcLoadingIndicator>
  </Story>
</Canvas>
