import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { IcLink, IcTypography, IcSectionContainer } from "../components";
import readme from "../../../web-components/src/components/ic-link/readme.md";
import { NavLink, MemoryRouter, Route, Routes } from "react-router-dom";

<Meta title="Link" component={IcLink} />

<Description markdown={readme} />

### Default

<Canvas>
  <Story name="Default" href="/">
    <IcLink href="/">This is a link</IcLink>
  </Story>
</Canvas>

### With icon

<Canvas>
  <Story name="With icon">
    <IcLink href="/" target="_blank">
      This is a link
    </IcLink>
  </Story>
</Canvas>

### React Router

export const HomePage = () => (
  <IcSectionContainer>
    <IcTypography variant="h1">Hi there!</IcTypography>
    <IcTypography>
      This example is demonstrating the link component with React Router
    </IcTypography>
  </IcSectionContainer>
);

export const DailyTippers = () => (
  <IcSectionContainer>
    <IcTypography variant="h1">Daily tippers</IcTypography>
    <IcTypography>
      Lorem ipsum doloe sit amet, consectetur adipiscing
    </IcTypography>
  </IcSectionContainer>
);

<Canvas>
  <Story
    name="With React Router"
    decorators={[
      (Story) => (
        <MemoryRouter initialEntries={["/"]}>
          <Story />
        </MemoryRouter>
      ),
    ]}
  >
    <IcLink style={{ paddingRight: "var(--ic-space-xs)" }}>
      <NavLink to="/" slot="router-item">
        Home
      </NavLink>
    </IcLink>
    <IcLink>
      <NavLink to="/daily-tippers" slot="router-item">
        Daily Tippers
      </NavLink>
    </IcLink>
    <Routes>
      <Route path="/" element={<HomePage />} />
      <Route path="/daily-tippers" element={<DailyTippers />} />
    </Routes>
  </Story>
</Canvas>

### Playground

export const defaultArgs = {
  href: "/",
  target: "",
  download: false,
  theme: "inherit",
  monochrome: false,
};

<Canvas>
  <Story
    args={defaultArgs}
    parameters={{ loki: { skip: true } }}
    argTypes={{
      target: {
        options: ["_blank", "_self", "_parent", "_top", ""],
        control: {
          type: "radio",
        },
      },
      theme: {
        options: ["inherit", "light", "dark"],
        control: { type: "inline-radio" },
      },
    }}
    name="Playground"
  >
    {(args) =>(
      <IcLink
        href={args.href}
        target={args.target}
        download={args.download === true}
        theme={args.theme}
        monochrome={args.monochrome}
      >This is a link</IcLink>
    )}
  </Story>
</Canvas>