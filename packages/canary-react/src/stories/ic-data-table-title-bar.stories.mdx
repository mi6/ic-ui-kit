import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";

import { IcDataTableTitleBar } from '../components';
import { IcLink, IcButton, IcTypography, IcChip } from "@ukic/react";
import readme from "../../../canary-web-components/src/components/ic-data-table-title-bar/readme.md";
import { SlottedSVG } from "@ukic/react/src/react-component-lib/slottedSVG";

import { mdiImage } from "@mdi/js";

<Meta
  title="React Components/Data Table Title Bar"
  component={IcDataTableTitleBar}
  parameters={{
    componentAPI: {
      data: readme,
    },
  }}
/>

# [Canary]: Data Table Title Bar

Use the Data Table Title Bar component to provide titling, description, metadata and also global table functionality. 

### Basic

A basic example with a heading.

By default, the density selector appears within the Data Table Title Bar. The three density options are:

- Default
- Dense
- Spacious

<Canvas>
  <Story name="Basic">
    <IcDataTableTitleBar heading="Title Bar" />
  </Story>
</Canvas>

#### Basic code example

```jsx
import * as React from "react";
import { IcDataTableTitleBar } from "@ukic/canary-react";

const DataTableTitleBar = () => (
  <IcDataTableTitleBar heading="Title Bar" />
)

export default DataTableTitleBar;
```

It is also possible to pass custom elements into the area which contains the title bar heading and description. This flexibility allows the heading tag to be updated if the heading prop is not semantically corrected based on your content structure. 

<Canvas>
  <Story name="Slots">
    <IcDataTableTitleBar>
      <IcTypography slot='heading' variant='h1'>
        <h3>Title Bar</h3>
      </IcTypography>
      <div slot='description'>
        <IcChip label='Chip' />
        <IcChip label='Chip' />
      </div>
    </IcDataTableTitleBar>
  </Story>
</Canvas>

```jsx
import * as React from "react";
import { IcDataTableTitleBar } from "@ukic/canary-react";
import { IcTypography, IcChip } from "@ukic/react";

const DataTableTitleBar = () => (
  <IcDataTableTitleBar>
    <IcTypography slot='heading' variant='h1'>
      <h3>Title Bar</h3>
    </IcTypography>
    <div slot='description'>
      <IcChip label='Chip' />
      <IcChip label='Chip' />
    </div>
  </IcDataTableTitleBar>
)

export default DataTableTitleBar;
```

### With metadata and description

Data table metadata and description can be added to the Data Table Title Bar via the `metadata` and `description` prop.

<Canvas>
  <Story name="With metadata and description">
    <IcDataTableTitleBar
      heading="Title Bar"
      metadata="128 items | 32gb | Updated: 01/02/03"
      description="Data table description that describes the purpose of the table."
    />
  </Story>
</Canvas>

### With metadata and description code example

```jsx
import * as React from "react";
import { IcDataTableTitleBar } from "@ukic/canary-react";

const DataTableTitleBar = () => (
  <IcDataTableTitleBar
    heading="Title Bar"
    metadata="128 items | 32gb | Updated: 01/02/03"
    description="Data table description that describes the purpose of the table."
  />
)

export default DataTableTitleBar;
```

### Hide density selector

By default, the density selector is displayed on the Data Table Title Bar. To hide it, set `hideDensitySelect` to `true`.

<Canvas>
  <Story name="Hide density selector">
    <IcDataTableTitleBar
      hideDensitySelect
      heading="Title Bar"
      metadata="128 items | 32gb | Updated: 01/02/03"
      description="Data table description that describes the purpose of the table."
    />
  </Story>
</Canvas>

```jsx
import * as React from "react";
import { IcDataTableTitleBar } from "@ukic/canary-react";

const DataTableTitleBar = () => (
  <IcDataTableTitleBar
    hideDensitySelect
    heading="Title Bar"
    metadata="128 items | 32gb | Updated: 01/02/03"
    description="Data table description that describes the purpose of the table."
  />
)

export default DataTableTitleBar;
```

### Custom actions

Custom actions can be added to the Data Table Title Bar via slots. 

There are two slots available:

- `primary-action` is suitable for an primary button. The custom element will appear to the right of the Data Table Title Bar. 
- `custom-actions` can be used for secondary actions. The custom element will appear to the left of the density selector if displayed.


<Canvas>
  <Story name="Custom actions">
    <IcDataTableTitleBar
      heading="Title Bar"
      metadata="128 items | 32gb | Updated: 01/02/03"
      description="Data table description that describes the purpose of the table."
    >
      <IcButton slot="primary-action" size="small">
        Primary
      </IcButton>
      <IcButton slot="custom-actions" variant="icon" aria-label="Icon Button">
        <SlottedSVG path={mdiImage} viewBox="0 0 24 24" />
      </IcButton>
    </IcDataTableTitleBar>
  </Story>
</Canvas>

```jsx
import * as React from "react";
import { IcDataTableTitleBar } from "@ukic/canary-react";
import { IcButton, SlottedSVG } from "@ukic/react";
import { mdiImage } from "@mdi/js";

const DataTableTitleBar = () => (
  <IcDataTableTitleBar
    heading="Title Bar"
    metadata="128 items | 32gb | Updated: 01/02/03"
    description="Data table description that describes the purpose of the table."
  >
    <IcButton slot="primary-action" size="small">
      Primary
    </IcButton>
    <IcButton slot="custom-actions" variant="icon" aria-label="Icon Button">
      <SlottedSVG path={mdiImage} viewBox="0 0 24 24" />
    </IcButton>
  </IcDataTableTitleBar>
)

export default DataTableTitleBar;
```
