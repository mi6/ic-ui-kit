import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { html } from "lit-html";
import readme from "./readme.md";

<Meta title="Web Components/Loading" component="ic-loading-indicator" />

<Description markdown={readme} />

export const defaultArgs = {
  description: "Loading",
  fullWidth: false,
  labelDuration: 8000,
  size: "medium",
  type: "circular",
  label: null,
  theme: "inherit",
};

export const defaultArgTypes = {
  label: {
    control: { type: "text" },
  },
  size: {
    options: ["small", "medium", "large", "icon"],
    control: { type: "radio" },
  },
  theme: {
    options: ["inherit", "light", "dark"],
    control: { type: "inline-radio" },
  },
  type: {
    options: ["circular", "linear"],
    control: { type: "radio" },
  },
};

### Circular (indeterminate)

<Canvas>
  <Story name="Circular (indeterminate)" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator label="Loading..."></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Circular (determinate)

<Canvas>
  <Story name="Circular (determinate)" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator
        progress="30"
        min="0"
        max="100"
        label="Loading..."
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Circular long label

<Canvas>
  <Story name="Circular long label" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator
        label="This is a label for a loading indicator that is indeterminate and should wrap onto more than one line when it gets too long"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Circular variants (indeterminate)

<Canvas>
  <Story
    name="Circular variants (indeterminate)"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator label="Small" size="small"></ic-loading-indicator>
        <ic-loading-indicator label="Default"></ic-loading-indicator>
        <ic-loading-indicator label="Large" size="large"></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Circular variants (determinate)

<Canvas>
  <Story
    name="Circular variants (determinate)"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Small"
          size="small"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Default"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Large"
          size="large"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Icon

<Canvas>
  <Story name="Icon" parameters={{ loki: { skip: true } }}>
    {(args) => html`<ic-loading-indicator size="icon"></ic-loading-indicator>`}
  </Story>
</Canvas>

### Linear (indeterminate)

<Canvas>
  <Story name="Linear (indeterminate)" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator
        label="Loading..."
        type="linear"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Linear (determinate)

<Canvas>
  <Story name="Linear (determinate)" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator
        progress="30"
        min="0"
        max="100"
        label="Progress"
        type="linear"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Linear long label

<Canvas>
  <Story name="Linear long label" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator
        type="linear"
        label="This is a label for a loading indicator that is indeterminate and should wrap onto more than one line when it gets too long"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Linear variants (indeterminate)

<Canvas>
  <Story
    name="Linear variants (indeterminate)"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator
          label="Small"
          size="small"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Default"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Large"
          size="large"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Full-width"
          size="medium"
          type="linear"
          full-width="true"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Linear variants (determinate)

<Canvas>
  <Story
    name="Linear variants (determinate)"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Small"
          size="small"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Default"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Large"
          size="large"
          type="linear"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Full-width"
          size="medium"
          type="linear"
          full-width="true"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Circular dark mode

<Canvas>
  <Story
    name="Circular dark mode"
    parameters={{ backgrounds: { default: "dark" }, loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator
          label="Small"
          size="small"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Default"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Large"
          size="large"
          theme="dark"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Icon dark mode

<Canvas>
  <Story
    name="Icon dark mode"
    parameters={{ backgrounds: { default: "dark" }, loki: { skip: true } }}
  >
    {(args) =>
      html`<ic-loading-indicator
        size="icon"
        theme="dark"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Linear dark mode

<Canvas>
  <Story
    name="Linear dark mode"
    parameters={{ backgrounds: { default: "dark" }, loki: { skip: true } }}
  >
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator
          label="Default"
          type="linear"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Large"
          size="large"
          type="linear"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          label="Full-width"
          type="linear"
          full-width="true"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Default"
          type="linear"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Large"
          size="large"
          type="linear"
          theme="dark"
        ></ic-loading-indicator>
        <ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          label="Full-width"
          type="linear"
          full-width="true"
          theme="dark"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### No label

<Canvas>
  <Story name="No label" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<div style="display: grid; gap: 50px;">
        <ic-loading-indicator description="Loading"></ic-loading-indicator
        ><ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          description="Loading"
        ></ic-loading-indicator
        ><ic-loading-indicator
          description="Loading"
          type="linear"
        ></ic-loading-indicator
        ><ic-loading-indicator
          progress="30"
          min="0"
          max="100"
          description="Loading"
          type="linear"
        ></ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Changing label

<Canvas>
  <Story name="Changing label" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`<ic-loading-indicator label-duration="4000"></ic-loading-indicator>
        <script>
          document.querySelector("ic-loading-indicator").label = [
            "Loading...",
            "Still loading...",
          ];
        </script> `
    }
  </Story>
</Canvas>

### Progress indicator for compact step

<Canvas>
  <Story
    name="Progress indicator for compact step"
    args={defaultArgs}
    argTypes={defaultArgTypes}
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<ic-loading-indicator
        size="small"
        inner-label="3"
        progress="30"
        min="0"
        max="100"
      ></ic-loading-indicator>`
    }
  </Story>
</Canvas>

### Playground - indeterminate

<Canvas>
  <Story
    parameters={{ loki: { skip: true } }}
    args={defaultArgs}
    argTypes={defaultArgTypes}
    name="Playground - indeterminate"
  >
    {(args) =>
      html`<div
        style="background-color: ${args.theme === "dark"
          ? "#393939"
          : "#FFFFFF"}; padding: 16px;"
      >
        <ic-loading-indicator
          theme=${args.theme}
          description=${args.description}
          full-width=${args.fullWidth}
          label-duration=${args.labelDuration}
          size=${args.size}
          type=${args.type}
          label=${args.label}
        >
        </ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>

### Playground - determinate

<Canvas>
  <Story
    parameters={{ loki: { skip: true } }}
    args={{
      ...defaultArgs,
      max: 100,
      min: 0,
      progress: 30,
    }}
    argTypes={defaultArgTypes}
    name="Playground - determinate"
  >
    {(args) =>
      html`<div
        style="background-color: ${args.theme === "dark"
          ? "#393939"
          : "#FFFFFF"}; padding: 16px;"
      >
        <ic-loading-indicator
          theme=${args.theme}
          description=${args.description}
          full-width=${args.fullWidth}
          label-duration=${args.labelDuration}
          size=${args.size}
          type=${args.type}
          label=${args.label}
          progress=${args.progress}
          min=${args.min}
          max=${args.max}
        >
        </ic-loading-indicator>
      </div>`
    }
  </Story>
</Canvas>
