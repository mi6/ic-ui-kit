import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { html } from "lit-html";
import readme from "./readme.md";
import Link from "../ic-link/readme.md";
import Button from "../ic-button/readme.md";

<Meta title="Web Components/Alert" component="ic-alert" />

<Description markdown={readme} />

export const defaultArgs = {
  dismissible: false,
  heading: "Heading",
  message: "Message",
  titleAbove: false,
  variant: "neutral",
};

### Variants

<Canvas>
  <Story name="Variants" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
        heading="Neutral"
        message="This alert is for displaying miscellaneous messages"
      ></ic-alert>
      <ic-alert
        variant="info"
        heading="Info"
        message="This alert is for displaying information"
      ></ic-alert>
      <ic-alert
        variant="error"
        heading="Error"
        message="This alert is for displaying errors"
      ></ic-alert>
      <ic-alert
        variant="warning"
        heading="Warning"
        message="This alert is for displaying warnings"
      ></ic-alert>
      <ic-alert
        variant="success"
        heading="Success"
        message="This alert is for displaying success messages. It has a very long message with lots of text which goes over multiple
        lines so the alert should expand as well to make sure it doesn't overflow"
      ></ic-alert>`}
  </Story>
</Canvas>

### Message only

<Canvas>
  <Story name="Message only" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
      message="This alert is for displaying miscellaneous messages"
    ></ic-alert>`}
  </Story>
</Canvas>

### Dismissible

<Canvas>
  <Story name="Dismissible" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
      heading="Neutral"
      message="This alert is for displaying miscellaneous messages"
      dismissible="true"
    ></ic-alert>`}
  </Story>
</Canvas>

### With action

<Canvas>
  <Story name="With action" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
      heading="Neutral"
      message="This alert is for displaying miscellaneous messages"
      ><ic-button slot="action" variant="secondary">Button</ic-button></ic-alert
    >`}
  </Story>
</Canvas>

### Dismissible with action

<Canvas>
  <Story name="Dismissible with action" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
      heading="Neutral"
      message="This alert is for displaying miscellaneous messages"
      dismissible="true"
      ><ic-button slot="action" variant="secondary">Button</ic-button></ic-alert
    >`}
  </Story>
</Canvas>

### Title above

<Canvas>
  <Story name="Title above" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
      heading="Neutral"
      message="This alert is for displaying miscellaneous messages"
      title-above="true"
    ></ic-alert>`}
  </Story>
</Canvas>

### Responsive title

<Canvas>
  <Story name="Responsive title" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert
        heading="This title is very long so should force the alert to add 'title-above'"
        message="This alert does not have 'title-above' added as a prop but it is forced to add it due to the length of the title"
      ></ic-alert>
      <ic-alert
        variant="success"
        heading="Shorter title"
        message="Neither of these has 'title-above' set to true"
      ></ic-alert>`}
  </Story>
</Canvas>

### Custom message

<Canvas>
  <Story name="Custom message" parameters={{ loki: { skip: true } }}>
    {html`<ic-alert heading="This alert uses a custom message slot">
      <ic-typography variant="body" slot="message">
        <p>
          This is some text and
          <ic-link href="/" inline>this is an inline link</ic-link> within the
          text.
        </p>
      </ic-typography>
    </ic-alert>`}
  </Story>
</Canvas>

### Custom message and title above

<Canvas>
  <Story
    name="Custom message and title above"
    parameters={{ loki: { skip: true } }}
  >
    {html`<ic-alert
      heading="Want to know more about our coffee?"
      title-above="true"
    >
      <span slot="message" style="font:var(--ic-font-body)">
        Go to our <ic-link href="/">coffee page</ic-link> to learn more.
      </ic-typography>
    </ic-alert>`}
  </Story>
</Canvas>

### Announced

<Canvas>
  <Story name="Announced" parameters={{ loki: { skip: true } }}>
    {html`
      <ic-alert
        id="alert"
        heading="Neutral"
        message="This alert is for displaying miscellaneous messages"
        announced="false"
      ></ic-alert>
      <br />
      <br />
      <ic-button variant="primary" size="small" onClick="onClick()" id="btn"
        >Toggle announced prop</ic-button
      >
      <script>
        const alert = document.getElementById("alert");
        const btn = document.getElementById("btn");
        btn.addEventListener("click", onClick);
        function onClick(e) {
          e.preventDefault();
          alert.announced
            ? (alert.announced = "false")
            : (alert.announced = "true");
        }
      </script>
    `}
  </Story>
</Canvas>

### Playground

<Canvas>
  <Story
    args={defaultArgs}
    argTypes={{
      variant: {
        options: ["neutral", "info", "warning", "error", "success"],
        control: { type: "select" },
      },
    }}
    name="Playground"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`<ic-alert
        dismissible=${args.dismissible}
        variant=${args.variant}
        heading=${args.heading}
        message=${args.message}
        title-above=${args.titleAbove}
      ></ic-alert>`
    }
  </Story>
</Canvas>
