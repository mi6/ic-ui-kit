import { Meta, Description, Canvas, Story } from "@storybook/addon-docs";
import readme from "./readme.md";
import { html } from "lit-html";

<Meta title="Web Components/Pagination bar" component="Pagination bar" />

<Description markdown={readme} />

### Variants

<Canvas>
  <Story name="Variants">
    {(args) =>
      html` <ic-pagination-bar total-items="100"></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          show-items-per-page-control="true"
        ></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          show-go-to-page-control="true"
        ></ic-pagination-bar>`
    }
  </Story>
</Canvas>

### Pagination bar type

<Canvas>
  <Story name="Pagination bar type">
    {(args) =>
      html`<ic-pagination-bar
          total-items="100"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          pagination-type="data"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>`
    }
  </Story>
</Canvas>

### Pagination control type

<Canvas>
  <Story name="Pagination control type">
    {(args) =>
      html`<ic-pagination-bar
          total-items="100"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          pagination-control="complex"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>`
    }
  </Story>
</Canvas>

### Alignment

<Canvas>
  <Story name="Alignment">
    {(args) =>
      html`<ic-pagination-bar
          total-items="100"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          alignment="left"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>
        <ic-pagination-bar
          total-items="100"
          alignment="space-between"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>`
    }
  </Story>
</Canvas>

### Appearance

<Canvas>
  <Story name="Appearance">
    {(args) =>
      html`<ic-pagination-bar
          total-items="100"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>
        <div style="background-color: var(--ic-architectural-black)">
          <ic-pagination-bar
            total-items="100"
            appearance="light"
            show-items-per-page-control="true"
            show-go-to-page-control="true"
          ></ic-pagination-bar>
        </div>
        <div style="background-color: var(--ic-status-info-contrast)">
          <ic-pagination-bar
            total-items="100"
            appearance="dark"
            show-items-per-page-control="true"
            show-go-to-page-control="true"
          ></ic-pagination-bar>
        </div>`
    }
  </Story>
</Canvas>

### Custom options

<Canvas>
  <Story name="Custom options">
    {(args) =>
      html` <script>
          const paginationBar = document.querySelector("ic-pagination-bar");
          paginationBar.itemsPerPageOptions = [
            { label: "50", value: "50" },
            { label: "100", value: "100" },
            { label: "250", value: "250" },
            { label: "All", value: "1000" },
          ];
        </script>
        <ic-pagination-bar
          total-items="1000"
          show-items-per-page-control="true"
          show-go-to-page-control="true"
        ></ic-pagination-bar>`
    }
  </Story>
</Canvas>
