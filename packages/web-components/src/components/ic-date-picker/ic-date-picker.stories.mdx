import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { html } from "lit-html";
import readme from "./readme.md";

<Meta title="Web Components/Date picker" component="Date Picker" />

<Description markdown={readme} />

### Default

<Canvas>
  <Story name="Default" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Large

<Canvas>
  <Story name="Large" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          size="large"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Small

<Canvas>
  <Story name="Small" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          start-of-week="1"
          size="small"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### With value

<Canvas>
  <Story name="With value" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          value="01/01/2023"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Open at date

<Canvas>
  <Story name="Open at date" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          helper-text="The calendar will open showing 25th December 2023, if not date selected"
          open-at-date="2023-12-25"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Alternative dialog label

<Canvas>
  <Story name="Alternative dialog label" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          new-dialog-label="true"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Start of week - Sunday

<Canvas>
  <Story name="Start of week - Sunday" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          start-of-week="0"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### With max and min set

<Canvas>
  <Story name="With max and min set" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <script>
          var dp = document.querySelector("#min-max-dp");
          dp.max = new Date("28 September 2024");
        </script>
        <ic-date-picker
          label="When would you like to collect your coffee?"
          id="min-max-dp"
          min="2023-07-28"
          date-format="MM/DD/YYYY"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Only dates in July 2023

<Canvas>
  <Story name="Only dates in July 2023" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          helper-text="Choose a date between the 1st and 31st of July"
          min="2023-07-01"
          max="2023-07-31"
          show-picker-today-button="false"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Days outside month hidden

<Canvas>
  <Story name="Days outside month hidden" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          show-days-outside-month="false"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Working weekdays only

<Canvas>
  <Story name="Working weekdays only" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <script>
          document.querySelector("#weekday-picker").disableDays = [0, 6];
        </script>
        <ic-date-picker
          id="weekday-picker"
          label="When would you like to collect your coffee?"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Disabled dates (until-now)

<Canvas>
  <Story
    name="Disabled dates (until-now)"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          disable-until-now
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Disabled dates (from-now)

<Canvas>
  <Story name="Disabled dates (from-now)" parameters={{ loki: { skip: true } }}>
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          disable-from-now
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>

### Today & clear buttons hidden

<Canvas>
  <Story
    name="Today & clear buttons hidden"
    parameters={{ loki: { skip: true } }}
  >
    {(args) =>
      html`
        <ic-date-picker
          label="When would you like to collect your coffee?"
          show-picker-today-button="false"
          show-picker-clear-button="false"
        ></ic-date-picker>
      `
    }
  </Story>
</Canvas>
