import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { html } from "lit-html";
import readme from "./readme.md";

<Meta title="Web Components/Link" component="ic-link" />

<Description markdown={readme} />

### Default

<Canvas>
  <Story
    name="Default"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<ic-link href="/">This is a link</ic-link>`}
  </Story>
</Canvas>

### Button

<Canvas>
  <Story
    name="Button"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<div style="padding: 8px">
      <ic-link button-style="true" appearance="dark">
        <a slot="router-item" href="/">Slotted link</a>
      </ic-link>
    </div>`}
  </Story>
</Canvas>

### With icon

<Canvas>
  <Story
    name="With icon"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<ic-link href="/" show-icon>This is a link</ic-link>`}
  </Story>
</Canvas>

### Dark

<Canvas>
  <Story
    name="Dark"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<ic-link href="/" appearance="dark" target="_blank"
      >This is a link</ic-link
    >`}
  </Story>
</Canvas>

### Light

<Canvas>
  <Story
    name="Light"
    parameters={{ backgrounds: { default: "dark" }, loki: { skip: true } }}
  >
    {html` <ic-link href="/" appearance="light" show-icon
      >This is a link</ic-link
    >`}
  </Story>
</Canvas>
