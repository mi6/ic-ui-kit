import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs";
import { html } from "lit-html";
import readme from "./readme.md";

<Meta title="Web Components/Link" component="ic-link" />

<Description markdown={readme} />

### Default

<Canvas>
  <Story
    name="Default"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<div>
      <ic-link href="/">This is a link</ic-link>
      <br />
      <br />
      <div style="width: 100px">
        <ic-link href="/"
          >This is a very very long link that goes multi-line</ic-link
        >
      </div>
    </div>`}
  </Story>
</Canvas>

### With icon

<Canvas>
  <Story
    name="With icon"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<ic-link href="/" target="_blank">This is a link</ic-link>`}
  </Story>
</Canvas>

### With download

<Canvas>
  <Story
    name="Download link"
    parameters={{ layout: "fullscreen", loki: { skip: true } }}
  >
    {html`<ic-link href="/" download>Download File</ic-link>`}
  </Story>
</Canvas>

### Playground

export const defaultArgs = {
  href: "/",
  target: "",
  monochrome: false,
  theme: "inherit",
};

<Canvas>
  <Story
    args={defaultArgs}
    parameters={{ loki: { skip: true } }}
    argTypes={{
      target: {
        options: ["_blank", "_self", "_parent", "_top", ""],
        control: {
          type: "radio",
        },
      },
      theme: {
        options: ["inherit", "light", "dark"],
        control: { type: "inline-radio" },
      },
    }}
    name="Playground"
  >
    {(args) =>
      html` <ic-link
        href=${args.href}
        target=${args.target}
        monochrome=${args.monochrome}
        theme=${args.theme}
        >This is a link</ic-link
      >`
    }
  </Story>
</Canvas>
